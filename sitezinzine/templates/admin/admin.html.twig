<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}
                Administration
            {% endblock %}
        </title>
        {% block stylesheets %}
           
            <link href="https://fonts.googleapis.com/css2?family=Montserrat+Alternates:wght@400;500;700&display=swap" rel="stylesheet">
            <link href="https://fonts.googleapis.com/css2?family=Montserrat+Alternates:ital,wght@0,400;0,500;0,700;1,400;1,500;1,700&display=swap" rel="stylesheet">
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
            <script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/fr.js"></script>
            <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
       
            <style>
            .main-content {
                background: linear-gradient(#E4013A, #B81C61);
                background-size: 100%;
                width: 100%;
                margin: 0;
                padding: 0;
                max-width: 100%;
                overflow-x: hidden;
                display: flex;
                flex-direction: column;
                font-family: "Montserrat Alternates", sans-serif;
                font-weight: 400;
                font-style: normal;
                min-height: 100vh;
            }
            .top img {
                height: auto;
                display: block;
                width: 100%;
            }
           
            em {
                font-style: italic !important;
            }
            
            strong {
                font-weight: 700 !important;
            }
            .main-content h1 {
                text-align: center;
                color: white;
                font-family: "Montserrat Alternates", sans-serif;
                font-weight: 900;
                font-style: normal;
                font-size: clamp(24px, 5vw, 36pt);
                margin: 20px auto; /* Pour centrer verticalement aussi */
            }
            .main-content h2 {
                text-align: center;
                color: #E40039;
                font-family: "Montserrat Alternates", sans-serif;
                font-weight: 900;
                font-style: normal;
                font-size: clamp(20px, 4vw, 26pt);
                margin: 20px auto; /* Pour centrer verticalement aussi */
            }
            .main-content h3 {
                text-align: center;
                color: #E40039;
                font-family: "Montserrat Alternates", sans-serif;
                font-weight: 900;
                font-style: normal;
                font-size: clamp(16px, 3vw, 20pt);
                margin: 20px auto; /* Pour centrer verticalement aussi */
            }

                    /* Styles des boutons */
        .btn-create {
            display: inline-block;
            text-decoration: none;
            padding: clamp(5px, 1vw, 10px) clamp(10px, 2vw, 20px);
            font-size: clamp(0.9em, 2vw, 1.2em);
            color: #000000;
            background-color: #ffffff;
            border-radius: 26px;
            -webkit-border-radius: 26px;
            -moz-border-radius: 26px;
            border: 4px solid black;
            box-shadow: 3px 7px 13px #444444;
            -webkit-box-shadow: 3px 7px 13px #444444;
            -moz-box-shadow: 3px 7px 13px #444444;
            transition: all 0.3s ease;
        }

        .btn-create:hover {
            padding: 5px;
            background-color: #E40039;
            color: #000000;
            border-radius: 24px;
            -webkit-border-radius: 24px;
            -moz-border-radius: 24px;
            box-shadow: 3px 7px 13px #777777;
            -webkit-box-shadow: 3px 7px 13px #777777;
            -moz-box-shadow: 3px 7px 13px #777777;
        }
         .btn-action {
            text-decoration: none;
            padding: clamp(4px, 1vw, 8px) clamp(8px, 2vw, 16px);
            font-size: clamp(0.8em, 1.5vw, 1em);
            color: #000000;
            background-color: #ffffff;
            border-radius: 26px;
            border: 2px solid black; /* Bordure plus fine */
            box-shadow: 2px 4px 8px rgba(0, 0, 0, 0.2); /* Ombre plus subtile */
            margin: 5px;
            cursor: pointer;
            display: inline-block;
            text-align: center;
            min-width: 60px;    /* Largeur minimum pour uniformité */
            max-width: 120px;   /* Largeur maximum pour éviter le dépassement */
            white-space: nowrap; /* Évite le retour à la ligne du texte */
    }

    .btn-action:hover {
        background-color: #E40039;
        color: white;
        transform: none;    /* Suppression de l'effet de grossissement */
        transition: background-color 0.3s, color 0.3s;
    }

    .btn-delete {
        border-color: #E40039;
    }

    .btn-delete:hover {
        background-color: #E40039;
        color: white;
    }

      /* Optimisation pour les conteneurs */
      .actions-buttons {
        display: flex;
        flex-direction: column;
        gap: 5px;
        align-items: flex-end; /* Aligne les boutons à droite */
    }

    .pagination {
        background-color: transparent;
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        gap: 5px;
        padding: clamp(5px, 1vw, 10px);
    }

    .pagination a:hover {
        padding: 14px 16px;
        color: grey;
        text-decoration: none;
        font-weight: 800;
    }
        /* Media Queries */
        @media (max-width: 768px) {
            .container {
                width: 100%;
            
            }
    
            .btn-create, .btn-action {
                display: block;
                width: 100%;
                max-width: none;
                text-align: center;
                margin: 5px 0;
            }
    
            .actions-buttons {
                flex-direction: column;
                align-items: stretch;
            }
        }
    
        @media (max-width: 480px) {
            .main-content h1 {
                font-size: 24px;
            }
    
            .main-content h2 {
                font-size: 20px;
            }
    
            .main-content h3 {
                font-size: 16px;
            }
    
            .pagination {
                font-size: 14px;
            }
        }


            </style>
        {% endblock %}
     {% block javascripts %}
    <!-- Charger le CDN de TinyMCE -->
   <script src="{{ asset('build/app.js') }}"></script>  <!-- Assure-toi que ce chemin est correct -->

{{ encore_entry_script_tags('app') }}
            {{ encore_entry_link_tags('app') }}
            {% block importmap %} 
                {{ importmap('app') }}
            {% endblock %}
        {% endblock %}
    </head>
    <body>
        {% include 'admin/navbar.html.twig' %}
        <div class="container my-4">
            {% include 'partials/flash.html.twig' %}
            <div class="main-content">
                <div class="top">
                    <img src="{{ asset('/images/bottom-header.gif') }}">
                </div>
                {% block body %}{% endblock %}
            </div>
        </div>
    </body>
</html>
